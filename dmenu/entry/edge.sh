#!/bin/sh

# usage: `export PATH=~/config_set/dmenu/entry/`
# if not work, try delete `~/.cache/dmenu_run` and run `dmenu_run` in terminal to re-generate dmenu cache

microsoft-edge-beta --proxy-server=socks5://127.0.0.1:10808
